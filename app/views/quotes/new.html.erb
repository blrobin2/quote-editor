<main class="container">
    <%= link_to sanitize(t('.back_button')), quotes_path %>

    <div class="header">
        <h1><%= t('.title') %></h1>
    </div>

    <%= render 'form', quote: @quote %>
</main>
